---
'@hyperdx/app': patch
---

Remove the obsolete spacer above inline SQL editors so the admin search header aligns SELECT and ORDER BY with the source selector and action buttons. Multiline editors continue to grow within the page layout.
