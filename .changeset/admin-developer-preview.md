---
'@hyperdx/app': minor
---

Let admins choose View as developer from the user menu to preview developer UI controls. Keep an accessible return-to-admin control in the sidebar, including when collapsed. Preview persists in the current browser tab and is scoped to the signed-in user and team without changing their database role.
