---
'@hyperdx/api': minor
'@hyperdx/app': minor
'@hyperdx/common-utils': patch
---

Restrict shared configuration management to configured admins while allowing developers to explore all team data. Start the filter sidebar with a small default list and persist personal pins per user and source across browsers.
